# MusicSync 开发进度记录

本文档用于记录开发过程中的阶段收口、页面演进、稳定性补强与近期已完成项。

## 1. 首页连接流程收口

- 即使远端还没选目录，连接也会成功建立
- 此时首页会进入“已连接，但远端目录未就绪”的状态

同时补上了远端目录状态的主动通知链路：

- 监听端在收到握手时，会记录当前对端会话
- 已连接状态下，若本机目录发生变化，会尽量把“目录已就绪 / 未就绪”主动通知给对端
- 对端收到该通知后，会把远端目录状态更新到本地

这一步的直接目标是让主流程更符合真实使用预期：

- 连接是连接
- 选目录是选目录
- 远端稍后选好目录后，本机不需要再重新点击连接

随后又继续收了一轮首页提示语义：

- “远端目录”状态不再一律写成“已加载索引 / 未加载索引”
- 现在会区分：
  - 尚未连接
  - 已连接但远端还没选目录
  - 远端目录已就绪，正在同步索引
  - 远端索引已可用
- 预览区也去掉了与正式远端流程无关的“本地调试目标未选择”提示噪音

这样做的目的，是避免正式主流程继续被开发期遗留概念干扰。

又补了一层对称前置条件提示：

- 如果远端已经就绪，但本地源目录还没选
  - 首页会明确提示“现在只差本地源目录”
- 如果本地源目录已经就绪，但远端还没选目录
  - 首页会明确提示“现在只差远端共享目录”

这样首页不再只会笼统地说“等待自动分析”，而是能直接指出当前缺的是哪一侧。

首页的按钮层级也继续做了一轮收口：

- 连接输入与连接按钮仍保留在主流程正面区域
- 监听开关、最近地址、发现设备这些“辅助连接”能力被收进折叠区
- 刷新远端索引被保留，但降级为次要操作，并补了使用时机提示

这样页面第一眼更聚焦于：

- 连接远端
- 选择本地目录
- 等待自动预览
- 执行同步

随后首页首屏又完成了一次更彻底的步骤一重构：

- 删除了顶部的“模式 / 当前方向 / 当前流程”三张说明卡
- 步骤一改为更紧凑的连接主控区
- 当前实现包含：
  - 连接状态胶囊
  - 监听端口胶囊
  - 分享按钮
  - 地址输入框
  - 右侧连接按钮
- 最近地址被保留在输入框下方
- 二维码分享弹窗已接入，便于另一台设备扫码或手动录入地址
- 监听相关操作不再以大段说明和折叠菜单占据首屏

这一步的目标是让首页第一屏更接近真正的工具产品，而不是开发期工作台。

随后又继续做了一轮步骤一细化：

- 状态胶囊已按语义分色
  - 未监听：警示色
  - 监听中：激活色
  - 已连接：成功色
- 颜色实现基于 `ColorScheme` 语义槽位，而不是硬编码品牌色
  - 这样后续如果整体切到 Material Design 配色，组件可以直接跟随主题
- 地址输入与连接按钮已调整为上下结构
  - 连接按钮横跨整行
  - 当已经连接时，按钮会切换为“停止连接”
- “刷新远端索引”已从步骤一移到步骤三，更符合当前心智顺序
- `localhost` 设备名问题也做了一轮兜底修正，尽量回退到更可识别的平台名

随后又补了一轮更接近 Material 语义的颜色与状态控制：

- 步骤一不再只是“改胶囊颜色”，而是整块按 `ColorScheme` 语义收口
  - 连接区内部背景：`surfaceContainerLow`
  - 空闲态胶囊：中性容器色
  - 监听 / 连接中胶囊：激活容器色
  - 已连接胶囊：成功语义的 `tertiaryContainer`
  - 主连接按钮：未连接时走 `primary`，可停止时切到次级容器色
- `连接中` 不再把连接按钮彻底锁死
  - 现在连接中也可以点击“停止连接”
  - 控制器增加了连接尝试代次，避免取消后旧连接结果再把状态写回来

这样既能改善当前交互，也更利于后续整体切到更规范的 Material Design 配色体系。

随后又继续做了一轮连接区去噪与直达操作：

- 步骤一不再显示远端根目录、索引时间、文件数这些细节
- “远端共享目录已就绪”提示被收短，不再夹带额外动作说明
- 最近地址现在改为点击即连，不再只是把地址回填到输入框

这一步的目标是让步骤一继续只承担“连接与进入同步流程”的职责，不去提前展示更适合步骤三的信息。

这一步的目标很明确：

- 缩短从“打开 App”到“拿到可执行预览”的点击路径
- 避免用户必须靠失败提示倒推自己漏了哪一步
- 让当前 UI 更符合第一版的真实产品边界：`本机 -> 远端`

步骤 1 当前已可视为收口完成，最终状态如下：

- 步骤 1 现在只承担“建立连接入口”的职责
  - 不再展示远端根目录、索引时间、文件数等属于后续步骤的信息
  - 首屏重点只保留：连接状态、监听端口、分享地址、地址输入与连接按钮
- 连接与远端目录状态已彻底解耦
  - 连接成功不再依赖远端是否已选择共享目录
  - 远端稍后选好目录后，可通过现有会话把“目录已就绪”状态推送给本机
  - 本机不需要再重新点击连接
- 连接主控区已按 Material 语义收口
  - 连接状态胶囊、监听端口胶囊、分享按钮、主连接按钮都已统一走 `ColorScheme`
  - 主按钮在“可连接”和“可停止连接”之间切换
  - 连接中也允许主动停止，避免用户被卡死在等待态
- 分享地址弹窗已完成第一版产品化
  - 已改为紧凑的 `Dialog`
  - 标题改为“分享地址”
  - 二维码与地址卡片居中
  - 地址使用更接近 Material 化 code block 的样式
  - 点击地址区域即可复制
  - 分享地址现在始终以“本机监听地址 + 监听端口”为准，不再误显示对端地址
- 辅助连接入口已继续简化
  - 最近地址点击后直接尝试连接
  - 自动发现到的局域网设备会自动过期清理，避免断开后长期残留
- 当前实现下与步骤 1 直接相关的小风险已处理
  - 分享按钮在未准备好监听地址时不会误导用户
  - 状态胶囊已补齐 disabled 态，避免视觉上仍像可点击的活跃控件

因此，后续如果没有新的交互方向变化，步骤 1 不应再单独返工；只需在未来统一主题或整体视觉升级时顺带微调。

## 2. 步骤 2 与最近记录管理

- 步骤 2 主体已从“说明 + 按钮”收为“源目录状态卡片”
  - 当前目录名作为主信息
  - 路径降级为辅助信息
  - 主操作只保留“选择目录”
  - “清除当前选择”已移到卡片右上角的轻量图标按钮
- “清理未完成传输残留”已改为按需显示
  - 只有检测到 `*.music_sync_tmp` 时才出现
  - 清理成功后会自动从卡片中收回
- 最近目录与最近地址都已补充统一的管理弹窗
  - 首页仍只保留轻量快捷入口
  - 点击管理按钮后可进入独立弹窗管理
  - 当前支持：拖拽排序、备注、删除
  - 最近地址的编辑已升级为“地址 + 备注”双字段编辑
- 最近记录的展示规则已统一
  - 无备注时：首页快捷入口与管理弹窗中都只显示主标题一行
  - 有备注时：主标题显示备注，副标题显示原始地址或目录信息
- 最近记录管理的交互样式已继续收口
  - 卡片整块可点击直接使用
  - 左侧为拖拽手柄
  - 编辑 / 删除操作与标题区域同列显示
  - 拖拽中的代理背景已恢复为透明，不再出现额外白底或过重阴影

当前判断是：步骤 2 的产品形态已经基本稳定，后续只需要处理少量观感微调与命名清理，不应再继续扩大交互范围。

## 3. 步骤 3 / 步骤 4 合并收口

首页主流程最近又做了一次结构收口：

- 原本分开的“步骤 3：差异预览”和“步骤 4：执行同步”已并为同一张工作卡片
- 卡片内部顺序现在固定为：
  - 自动分析状态
  - 远端索引手动刷新
  - 预览摘要
  - 差异列表与类型筛选
  - 执行状态与进度
  - 执行 / 停止 / 查看结果
- “计划项”不再作为独立区块悬在执行区之外，而是直接作为同一上下文内的差异明细

这样调整的原因很直接：

- 对当前产品来说，“看差异”和“立即同步”本来就是同一决策动作
- 单独拆出一个步骤 4 只会重复展示上下文，增加页面纵向噪音
- 合并后更接近真实心智模型：准备好目录后，用户只需要在一处完成检查和执行

同时，这轮还顺手清理了几类遗留问题：

- 步骤 1 中不再显示“远端共享目录已就绪 / 未就绪”这类过细的动态提示文案
- 步骤 1 与步骤 2 的最近记录编辑入口已统一成更一致的 Material 对话框样式
- 一批已经脱离正式流程的旧文案键与命名已开始收口，避免后续维护时继续把首页误当成多页调试流的拼接产物

截至当前阶段，可以把首页正式理解为三段主流程：

- 步骤 1：连接远端设备
- 步骤 2：选择本地源目录
- 步骤 3：检查并同步

后续除非同步策略本身发生变化，否则首页主流程不应再拆回四段。

## 4. 步骤 3 近期收口

最近一轮首页工作主要集中在步骤 3“检查并同步”的信息密度和交互一致性上。

已完成的调整包括：

- 预览空态和有内容态被拆开处理
  - 没有计划项时，不再提前展示摘要卡和列表控制项
  - 当前只显示居中的等待或空结果提示
  - 有计划项后，才显示筛选区、列表区和执行区
- 传输方向提示已从“局域网预览 / 本地调试预览”改为真实设备方向
  - 当前显示为 `传输方向：设备 A -> 设备 B`
  - 本机设备名沿用连接层已有的主机名兜底逻辑
  - 远端设备名直接来自当前连接会话
- 执行区已进一步压缩
  - 主按钮当前采用 `开始同步 / 停止同步` 单按钮切换
  - 独立停止按钮已移除
  - 执行状态、进度条、文件进度和当前文件被收进更紧凑的单块信息面板
- 列表筛选能力已重做为“本地前端筛选”
  - 不再因为切换筛选就重建远端预览
  - 底层预览始终保留全量结果，页面只负责显示过滤

筛选交互当前状态如下：

- `项目类型`
  - 支持 `全部项目 / 复制项 / 删除项 / 冲突项`
  - `全部项目` 已调整为独立总览态
  - 它不会再视觉上把三类具体项一并勾上
  - 点击具体项后，会自动退出 `全部项目`
- `文件类型`
  - 支持多选
  - `全部类型` 为特殊总览态
  - 选择具体后缀后，会自动退出 `全部类型`
  - 所有具体后缀取消后，会自动回到 `全部类型`

列表表现也做了一轮收口：

- 列表容器新增边框和背景，和外部卡片形成明确分层
- 单项自身也有轻量边界，避免内容直接漂浮在列表区域里
- 右侧差异徽标已完成 i18n 化，不再直接显示硬编码英文
- 即使当前筛选无条目，列表容器仍然保留，只在容器内部显示“当前筛选无条目”

这轮调整的目的不是继续叠功能，而是把步骤 3 从“技术上可用”进一步收口到“产品上可读、可筛、可执行”。

## 5. 忽略后缀规则

设置页最近新增了一组正式规则配置：

- `忽略后缀`

当前实现方式是：

- 设置页显示摘要
  - 无规则时显示“当前没有忽略任何后缀”
  - 有规则时显示已忽略的后缀数量
- 点击后进入独立管理弹窗
  - 可添加后缀
  - 可删除后缀
  - 保存后持久化到本地设置存储

更重要的是，这组规则当前已经接入正式预览构建链路：

- 被忽略的后缀不会进入预览构建
- 也不会进入同步计划
- 因此它不是页面级的临时筛选，而是全局生效的同步规则

为了降低理解成本，步骤 3 当前也已补上轻量提示：

- 在“文件类型”标题右侧显示 `已忽略：.x, .x`
- 只有当设置中真的存在忽略规则时才显示

这使“全局忽略规则”和“当前页面临时筛选”终于被分层表达：

- 设置页的忽略后缀：决定哪些文件根本不进入计划
- 步骤 3 的文件类型筛选：只决定当前计划怎样显示

## 6. 设置页与错误模型收尾

- 设置页结构继续规范化
  - 返回按钮已并入标题左侧，和页面标题形成一行头部
  - `常规` 分组标题已轻微右移，和内容卡片对齐得更自然
  - 设置页的 joined card 组件已抽到 `features/settings/presentation/widgets/settings_group.dart`
  - 后续新增设置项时，只需要继续往 `SettingsJoinedGroup` 中追加 `SettingsActionRow`
- 错误文案从状态层继续抽离
  - `connection / directory / preview / execution` 这四个 state 不再直接保存中文用户文案
  - 当前先统一输出标准错误键或清洗后的兜底文本
  - 页面层通过 `AppErrorLocalizer` 再映射到 `l10n`
  - 这样英文界面不会再因为状态层硬编码而混入中文

## 7. 命名与目录收口

- 文件命名语义增强
  - `lib/l10n/app_localizations_x.dart` 已重命名为 `lib/l10n/app_localizations_ext.dart`
  - 原文件名中的 `x` 语义过弱，不利于长期维护时快速理解用途
- 测试目录层级统一
  - `test/protocol_codec_test.dart` 已移动到 `test/unit/network/protocol_codec_test.dart`
  - `test/widget_test.dart` 已移动并改名为 `test/widget/app_boot_test.dart`
  - 现在测试目录结构更一致：`unit` 放逻辑测试，`widget` 放界面测试
- 导入路径统一
  - 所有 `app_localizations_x.dart` 导入已同步替换为 `app_localizations_ext.dart`

## 8. 设置页结构化收尾

- 间距尺度继续收口
  - 页面与弹窗中的常用间距已统一为常量（`S/M/L`），减少硬编码数值分散
  - 现在调整设置页密度时，只需要改顶部常量，不需要在控件树里逐个查找
- 交互细节抽离
  - 开关拇指图标状态逻辑已提取为私有方法，避免主 build 逻辑继续膨胀
  - `Switch` 已支持选中 `check`、未选中 `close` 的图标语义
- 主题化一致性
  - `Switch` 的轨道/拇指颜色已统一走 `app_theme.dart` 中的 `switchTheme`
  - 页面局部只保留行为级配置，不再重复做视觉配色判断

## 9. 设置文案与忽略类型弹窗规范化

- 文案中英同步
  - 中文更新后，英文文案已同步到同一语义
  - 避免出现中英文语义不一致导致的后续评审反复
- 忽略文件类型弹窗结构规范化
  - 从自定义 `Dialog + 手工底部按钮区` 收口为更标准的 `AlertDialog` 结构
  - 标题 / 内容 / 操作按钮分区更明确
  - 内容区仍保留输入、列表、删除等原有能力
  - 间距继续复用设置页既有尺度常量，避免新增散落 hardcode

## 10. 设置页新增外观分组

- 新增分组与条目
  - `主题模式`：浅色 / 深色 / 跟随系统
  - `调色板`：Neutral / Expressive / Tonal Spot
  - 两个条目都通过 `settings_dialog_shell` 打开选择弹窗
- 状态与存储
  - `SettingsState` 新增 `themeMode` 与 `palette`
  - `SettingsStore` 新增对应读写键并持久化到 `SharedPreferences`
  - `SettingsController` 新增 `setThemeMode` / `setPalette`
- 应用生效链路
  - `MusicSyncApp` 现在会读取设置并应用：
    - `themeMode` -> `ThemeMode`
    - `palette` -> `DynamicSchemeVariant`
  - `AppTheme` 新增 `dark(...)` 并支持按 palette 生成亮/暗主题

另外，这轮顺手把弹窗里的单选控件切换到了新版 `RadioGroup` 写法，清理了 Flutter 3.41 的弃用告警。

## 11. 首页步骤 3 预览区收口

- 顶部状态表达改为“标签 + 胶囊值”
  - `传输方向`：胶囊中只显示 `A -> B`
  - `目录状态`：胶囊中显示 `本地 | 远端` 两段状态，并通过图标直接表达是否就绪
- 之前独立的一句等待文案已移除
  - 不再额外占据一行说明文字
  - 目录是否准备好，统一由 `目录状态` 胶囊承担表达
- 预览与执行动作进一步并拢
  - `开始同步 / 停止同步`
  - `生成远端预览`
  - `查看结果`
  - 执行进度也回收到同一操作区下方，而不是单独占一张远离的状态卡
- 计划列表进一步分层
  - 主列表只承载可执行项（复制 / 删除）
  - 冲突项从主筛选体系中独立出去，作为单独的冲突提示区长期显示
  - 即使当前切换到 `全部类型 / 复制项 / 删除项`，冲突提示也不会被隐藏

## 12. 冲突展示与后续媒体判定方向

当前冲突判定逻辑仍然保持最小实现：

- 在 `diff_engine.dart` 中
- 同路径文件双方都存在
- 只要 `size` 或 `modifiedTime` 不一致
- 就标记为 `conflict`
- 当前原因固定为 `metadata_mismatch`

这一轮没有继续把冲突系统扩展到音乐元数据层，而是先做了展示优化：

- 冲突列表的右侧徽标不再仅显示“冲突”
- 现在会直接显示原因的人类可读文案
  - 当前已接入：`metadata_mismatch -> 元数据不同`
- 列表正文不再把底层英文 reason 拼接到文件名后方
  - 这样在窄屏和手机上更容易读清文件名本身

已确认但暂缓的后续方向：

- 音乐文件冲突不应只看文件大小与修改时间
- 更有价值的是引入：
  - 标签完整度（标题、歌手、专辑、歌词、封面等）
  - 音质信息（码率、采样率、位深、编码格式等）

## 13. 预览详情面板第一阶段

这一轮开始补齐“预览列表单项详情”的最小可用能力，目标不是一次做成完整媒体信息面板，而是先让用户点开条目后能看到真正有价值的结构化信息。

当前已完成：

- 预览列表中的条目现在支持点击打开底部详情面板
- 详情面板的数据结构已从 `DiffItem` 抽离为独立的 `DiffItemDetailViewData`
  - 这样后续本地详情、远端详情、音频元数据都可以继续增量扩展，而不把 UI 直接绑死在差异模型上
- 详情面板会在打开后按需刷新条目详情
  - 本地条目：通过 `FileAccessGateway.stat()` 刷新
  - 远端条目：通过当前网络会话请求 `statEntry`
- 本地音频元数据已接入第一版按需读取
  - 当前先支持：标题、歌手、专辑、歌词
  - 读取时机为“打开详情后按需读取”，不会增加预览阶段的扫描成本
- 已修复一类常见的 MP3 标签乱码问题
  - 当文件同时存在 `ID3v1` 和 `ID3v2` 标签时，当前会优先使用更可信的 `ID3v2`
  - 避免旧版标签把新版正常中文覆盖掉
- 本地音频标签读取已补一层保守回退
  - 先快速读取前部标签并优先尝试 `ID3v2`
  - 未命中时，再做一次带超时保护的回退，补读 `ID3v1`
  - 这样既降低了详情打开卡顿风险，也尽量保住本地 MP3 的标题 / 歌手 / 专辑命中率
- 详情面板的信息层次也做了一轮收口
  - 条目卡片优先显示：`名称 / 位置 / 路径 / 文件大小 / 修改时间 / 条目类型`
  - 不再只把 `entryId` 直接暴露给用户
  - 开发期的“后续再加载更多元数据”占位提示已移除，避免产品层误导
- 歌词字段已补轻量折叠
  - 默认折叠为固定行数
  - 可在详情面板内直接展开 / 收起
  - 当前只作用于歌词，不影响其他字段展示

当前刻意没有做的内容：

- 远端音频标签解析
- 比特率 / 采样率 / 位深 / 编码格式
- 封面预览
- 源端与目标端更强的并排对比布局

## 14. 术语统一与路径展示收口

- 术语统一
  - 连接语境统一使用：`本机 / 远端设备`
  - 同步语境统一使用：`源端 / 目标端`
  - 这样“网络位置”和“同步角色”不再混在同一句话里
  - 当前只调整用户可见文案，不批量修改内部变量名，避免为了命名洁癖引入逻辑风险
- Android 路径展示
  - 底层文件访问仍继续使用原始 `entryId` / `content://...`
  - UI 展示层新增了轻量路径格式化
  - 会把常见的 Android SAF 路径收口成更接近人类可读的形式，例如：
    - `content://.../tree/primary%3Atest` -> `Internal storage/test`
    - `primary:test/song.mp3` -> `Internal storage/test/song.mp3`
  - 这样详情面板、首页目录摘要、最近目录记录都不再直接暴露原始 URI

同时，这轮还顺手补齐了详情链路的稳态兜底：

- 详情刷新现在默认“能打开优先”
  - 本地 `stat`
  - 远端 `statEntry`
  - 本地音频标签读取
  这三类操作都已加上超时保护
- 任意一项超时或失败时：
  - 不会把整个详情面板一起拖住
  - 当前会保留已有值继续显示
- 对应也已补充回归测试：
  - 本地标签读取卡住时快速返回
  - 本地 `stat` 卡住时保留旧值
  - 远端 `statEntry` 失败时保留旧值
  - Android 路径格式化的多种输入形态

## 15. 远端详情补齐第一版音频标签

这一轮没有继续扩大预览主流程，而是把“详情面板两侧信息不对称”的问题先补平一层。

当前已完成：

- 远端详情请求不再只返回基础 `stat`
  - 当前会在监听端按需读取音频标签
  - 再通过现有 `statEntry` 链路把标签一并返回给请求端
- 本地 / 远端详情现在复用同一套音频标签读取实现
  - 标签读取服务已从 `preview` feature 抽到更通用的位置
  - 避免后续继续复制一套“远端专用解析逻辑”
- 详情刷新链路已补成“本地返回本地详情对象，远端返回远端详情对象”
  - 不再把远端结果先压回 `FileAccessEntry`，再由 UI 二次拼装
  - 这样后续如果继续加比特率、采样率等字段，扩展点会更直接

这一轮之后，以下场景已经可以显示远端侧的标题 / 歌手 / 专辑：

- 远端独有条目
- 删除项中的目标端文件
- 任何 source/target 标记为远端的预览详情条目

同时已补回归测试覆盖：

- 远端详情刷新成功时，会带回音频标签
- 监听端整条请求链可返回远端音频元数据
- 远端详情请求超时后，会保留已有条目而不是拖住详情面板
- 远端目录条目不会误返回音频标签

另外，这一轮还补了一条和正式主流程强相关的状态语义回归：

- 默认执行“刷新远端索引”时
  - 会清掉旧预览
  - 会清掉旧执行结果
  - 但会保留当前目标根目录

这样用户在远端内容变化后重新进入分析流程时，不会把上一轮同步结果误当成当前上下文的一部分。

## 16. 忽略后缀规则归一化

这一轮把忽略后缀的规则处理统一到了同一套归一化逻辑，重点不是扩入口，而是避免规则越用越乱。

当前已完成：

- 新增统一的后缀归一化工具
  - 去首部 `.` 
  - 统一小写
  - 去掉前后空白
- 以下链路现在复用同一份规则：
  - 设置页忽略类型弹窗输入
  - 设置持久化读写
  - 预览构建时的忽略过滤
- 因此像 `.lrc` / `lrc` / `..LRC` 这类输入，现在都会归一成同一条规则

同时补了回归测试：

- 设置控制器保存并重载归一化后的忽略后缀
- 预览构建时可正确识别带 `.` 的忽略规则

## 17. 首页结构拆分第一阶段

这一轮没有继续扩功能，而是把首页正式往“装配页”方向收。

当前已完成：

- 步骤 1 连接区已拆到独立组件目录
  - `widgets/connection_section/connection_section.dart`
- 步骤 2 源目录区已拆到独立组件目录
  - `widgets/source_directory_section/source_directory_section.dart`
- 步骤 3 预览与执行工作区已拆到独立组件目录
  - `widgets/preview_workbench_section/preview_workbench_section.dart`
- 最近记录卡片也已抽出复用组件
  - `widgets/recent_record_card/recent_record_card.dart`

同时，首页内部两类较长的主流程匿名回调也已收为明确方法：

- `_buildRemotePreview(...)`
- `_executeRemoteSyncFlow(...)`

这一轮之后，`home_page.dart` 的职责已经更明确：

- 监听跨区状态
- 组装 3 个主流程区块
- 保留少量跨区协调逻辑

也就是说，首页已经不再是“一个超大 UI 文件”，而开始具备后续继续维护和扩展的结构基础。

## 18. 步骤 3 信息密度收口第二阶段

这一轮继续收口首页步骤 3，但仍然不改同步底层逻辑，目标是把预览/执行区做得更接近正式主流程。

当前已完成：

- 顶部状态语义继续压缩
  - `传输方向` 调整为 `传输状态`
  - 状态胶囊改为设备 A 与设备 B 之间的图标关系
  - 已连接时显示传输箭头
  - 未连接时显示断开图标
- 去掉了重复的中性提示
  - 顶部不再重复显示“未连接 / 未选目录”这类等待文案
  - 目录与连接准备度交由状态胶囊表达
- 执行区继续减噪
  - 去掉重复的执行状态胶囊
  - 没有当前传输文件时，不再显示占位路径文案
- 分组 / 文件类型筛选头部改成同一套结构
  - 为后续继续压缩样式和统一交互留出基础

这一轮之后，步骤 3 顶部已经更明确地表达为：

- 当前连接关系
- 当前目录准备度
- 需要用户处理的异常或警告
- 生成预览 / 开始同步 / 查看结果

## 19. 弹窗输入控制器生命周期收口

这一轮顺手修了一类潜在稳定性问题：若弹窗关闭后仍有一帧重建，函数级 `TextEditingController` 提前 `dispose()` 可能导致页面卡死或抛出异常。

已确认并处理：

- 设置页“忽略文件类型”弹窗
  - 已改为独立 `StatefulWidget`
  - 控制器与弹窗生命周期绑定
- 首页端口设置弹窗
- 首页最近记录备注弹窗
- 首页最近地址编辑弹窗

统一后的原则是：

- 输入控制器由弹窗内部状态持有
- 在弹窗组件 `dispose()` 时释放
- 外层只接收对话框返回值，不再直接管理控制器

这类调整不改变现有交互，但能降低“关闭弹窗后随机崩溃 / 卡死”的风险。

## 20. 远端目录失效时的执行状态修复

这一轮补了一处主流程级的状态修复：

- 当电脑作为本机、手机作为目标端执行同步时
- 如果手机在步骤 2 清除了目标目录
- 之前电脑侧会出现“预览被清空，但执行区像是卡住”的状态

当前已调整为：

- 远端目录失效后，预览仍会按预期清空
- 但若此时正在执行远端同步
  - 不再把执行态静默清掉
  - 会显式中止当前远端执行
  - 保留当前进度
  - 进入失败态并显示明确错误文案

这次修复的目标不是保留执行，而是避免用户看到“像还在跑、实际已经无效”的假状态。

## 21. 步骤 3 工作区最终样式收口

这一轮把步骤 3 从“多块并列的工具区”收口到了更稳定的正式样式，重点是结构定型，而不是继续扩交互。

最终落地结果：

- 顶部状态区
  - `传输状态` 与 `目录状态` 固定为上下排列
  - 连接关系改为设备名 + 图标语义
  - 去掉重复的等待提示
- 执行区
  - 执行状态与进度保留
  - 去掉重复状态芯片与无意义占位文案
- 筛选区
  - `分组` 与 `文件类型` 合并为单筛选块
  - 筛选块作为结果区内部顶部内容，而不是独立漂浮在外部
  - 最终去掉额外包裹感，仅保留轻量排版
- 列表区
  - 主列表恢复为“整体一张卡”
  - 内部条目改为紧贴排列，用分隔线区分
  - 筛选块与列表成为同一工作面的上下结构
  - 列表与外层边框、内层列表面的圆角职责重新拆清

这轮之后，步骤 3 的主线表达稳定为：

- 状态
- 操作
- 筛选
- 结果

也就是说，这一块已经从“探索期 UI”进入“可以围绕细节做小修”的阶段，后续不建议再做大结构摇摆。

## 22. 首页结构继续拆分与步骤 3 列表高度修正

这一轮没有新增功能，重点是继续降低 `home_page.dart` 的体积，同时修正步骤 3 预览列表的一处高度计算问题。

已完成：

- 首页结构继续拆分
  - 连接相关行为继续收口到
    - `widgets/connection_section/connection_section_actions.dart`
  - 最近记录管理弹窗已独立为
    - `widgets/recent_record_managers/recent_record_managers.dart`
  - 分享地址 / 备注 / 地址编辑弹窗已独立为
    - `widgets/home_dialogs/home_dialogs.dart`
  - 步骤 3 计划区已独立为
    - `widgets/preview_workbench_section/preview_plan_section.dart`
  - 步骤 3 的筛选、远端预览、执行后刷新等行为已开始收口到
    - `widgets/preview_workbench_section/preview_workbench_actions.dart`

- 步骤 3 的局部展示状态继续下沉
  - 冲突项展开 / 收起状态不再由首页持有
  - 已收进 `PreviewPlanSection` 自身管理

- 步骤 3 无列表时的底部空白已收口
  - 只在存在计划区时才插入状态区与列表区之间的分隔间距
  - 去掉了无列表状态下多余的底部留白

- 预览列表高度计算已修正
  - 之前 `PlanItemList` 会用写死值为筛选区 header 预留高度
  - 当筛选区实际高度超过预留值时，会挤压列表可见区域
  - 表现为“条目不多，但只显示很少几项且出现滚动条”
  - 当前已改为：
    - header 使用自然高度
    - 列表区域单独按自身高度计算
    - `maxHeight` 恢复为真正控制列表上限的参数

这一轮之后，首页主文件的职责继续变得更清楚：

- `home_page.dart`
  - 负责页面拼装、跨区状态监听、少量协调逻辑
- 各分区组件
  - 负责各自展示
- 分区 actions / dialogs / managers
  - 负责局部行为和弹窗实现

也就是说，首页已经不只是“拆 UI”，而是在逐步形成后续可持续维护的职责边界。

## 23. 同步异常收口与前台恢复自检

这一轮没有继续扩主流程功能，而是集中补了几条真正会影响发布稳定性的异常收口链路。

已完成：

- 本地源目录失效时的执行收口
  - 当本机作为源端、远端同步正在执行中时
  - 如果步骤 2 的本地源目录被清除或失效
  - 之前执行态会被直接清回 `idle`
  - 现在会显式进入失败态，并保留当前进度与错误语义

- 远端目录失效时的进度回写保护
  - 当目标端目录被清除或不可访问后
  - 执行态虽然会被收口成失败，但底层迟到的进度回调之前仍可能把状态重新写回 `running`
  - 当前已增加“仅当前有效执行令牌可更新进度”的保护
  - 避免首页继续卡在“像还在同步”的假运行态

- 被动断连时的远端执行收口
  - 如果同步过程中连接断开
  - 当前不再一律把执行区清成空白
  - 若当时正在执行远端同步，会显式进入失败态
  - 同时清空预览，连接状态切换为 `disconnected`

- 前台恢复自检
  - 首页已接入应用生命周期监听
  - App 从后台回到前台时
  - 若页面仍显示为已连接、且当前没有执行中的任务
  - 会主动探测一次远端状态
  - 如果连接实际上已经失效，会自动从“假连接”收口到真正的断开状态

这一轮的目标很明确：

- 不追求 Android 后台保活本身
- 先把“目录失效 / 目标失效 / 被动断连 / 前台恢复”这几类真实异常收口做稳
- 避免用户看到：
  - 进度像卡住
  - 按钮长期被锁
  - 页面显示已连接但实际已经断开

对应已补回归测试覆盖：

- 本地源目录清除时，运行中的远端执行进入失败态
- 远端目录失效时，迟到进度不会覆盖失败态
- 被动断连时，运行中的远端执行进入失败态
- 前台恢复探测时，若连接实际已断开，会收口到 `disconnected`

下一阶段再进入更大的 Android 专项：

- 后台尽量保持连接
- 必要时引入前台服务 / 通知保活

## 24. Android 后台连接保活第一步（前台服务骨架）

这一轮开始进入 Android 后台稳固专项，但范围控制在最小可验证版本：

- 目标是“提升后台期间连接存活概率”
- 暂不把同步逻辑整体搬入服务进程
- 先建立前台服务与 Flutter 生命周期的可控桥接

已完成：

- Android 侧新增前台服务骨架
  - `ConnectionKeepAliveService`
  - 服务启动后以低优先级通知常驻
  - 通知支持点击回到 App
- Flutter 侧新增原生运行时通道
  - 用于启停 Android 保活服务
  - 当前保持 best-effort，不让通道异常反向拖垮同步流程
- 首页生命周期接入启停策略
  - App 进入后台且仍处于已连接状态时，启用保活服务
  - App 回到前台时，关闭保活服务
  - 连接状态变化时也会同步更新服务开关

本轮踩到并修复的一处关键崩溃：

- Android 14+（targetSdk 36）在 `foregroundServiceType="dataSync"` 下
  - 仅声明 `FOREGROUND_SERVICE` 不够
  - 还必须声明 `FOREGROUND_SERVICE_DATA_SYNC`
- 否则切后台时会抛 `SecurityException` 并导致进程退出
- 当前已补齐权限声明，并在原生通道层增加了 `SecurityException` 兜底

当前阶段的判断标准：

- 切后台后不再因权限缺失直接崩溃
- 前台服务通知可见
- 回前台后连接状态仍能继续按既有链路自检与收口

下一步再做：

- 保活通知文案进一步产品化（多语言资源对齐）
- 评估“仅连接保活”与“同步进行中强保活”的差异策略

## 25. Android 后台保活第一阶段收口

这一轮没有继续扩大到“真正后台化同步”，而是先把前一阶段已经接入的保活骨架收成更符合当前产品边界的策略。

已完成：

- 保活启用条件进一步收紧
  - 不再采用“只要后台且已连接就保活”的宽策略
  - 当前改为：
    - Android
    - App 不在前台
    - 已连接
    - 正在执行远端同步
  - 也就是说，保活当前只为“后台期间正在进行的正式远端同步”服务

- 首页补了轻量可见提示
  - 首页左上新增不扰动现有排版的悬浮标签
  - 当前在以下条件成立时显示：
    - Android
    - 已连接
    - 正在执行远端同步
  - 文案使用 `后台保活已就绪`
  - 这样用户在前台就能明确知道：如果此时切后台，会进入保活策略

- 目录状态术语顺手做了一次去歧义
  - 步骤 3 的 `目录状态` 胶囊中
  - 原先固定写成 `本地 | 目标端`
  - 当前调整为 `本地 | 远端`
  - 这样在双端冷启动、同步角色尚未完全建立时，不会让“目标端”这个词过早暗示错误的角色语义

这一步的边界也同时明确下来：

- 当前实现提升的是“后台期间的存活概率”
- 还不是把连接与同步真正迁移到 Android `Service` 中独立托管
- 因此它能显著改善“切后台即掉”的问题，但还不能把后台行为定义成完全可靠的系统级守护

当前收口后的判断标准：

- Android 在前台执行远端同步时，可见 `后台保活已就绪`
- 切到后台后，前台服务通知生效
- 回到前台后，连接状态仍走既有自检与异常收口链路

## 26. 步骤 1 收口与发现链路补强

这一轮重新把首页步骤 1 收回到“设备会话入口”的产品定位，重点不是扩连接功能，而是降低首屏解释成本，并把 discovery 行为补到更接近正式产品可用的状态。

已完成：

- 步骤 1 主区继续减重
  - 主区只保留：
    - `附近的设备`
    - 已发现设备列表
    - 空设备骨架占位
    - `更多连接方式`
  - 监听状态胶囊已移动到顶部栏
  - 地址输入、最近地址、分享、端口设置继续下沉到二级面板

- 步骤 1 文案与空态收口
  - `发现到的设备` 改为 `附近的设备`
  - 空列表不再显示“暂时还没有搜索到设备”
  - 改为单卡片骨架占位，并与真实设备卡片保持相同结构层级
  - 步骤 1 顶部小刷新按钮语义已明确为 `更新连接状态`

- discovery 链路兼容性补强
  - 广播发送不再只依赖单一 `255.255.255.255 + anyIPv4`
  - 当前会枚举可用 IPv4 网卡，按接口发送广播
  - 会尽量跳过 `tun / tap / wintun / clash / vpn / virtual / vEthernet` 等明显虚拟接口
  - 这一轮实测修复了：
    - Windows 开启 Clash TUN 时，手机无法发现电脑，但手动地址连接仍可成功

- discovery 下线语义补齐
  - 手动停止监听时，会先主动广播一次 `goodbye`
  - 其他设备收到后，会立即把该设备从“附近的设备”中移除
  - 原有约 8 秒超时清理继续保留，作为异常情况下的兜底

- 刷新按钮语义收紧
  - 当前刷新不会因为 discovery 未命中就误伤已连接设备
  - 已连接设备会保留在“附近的设备”列表中
  - 只有连接层明确确认断开时，才真正切换连接状态
  - 也就是说，`附近设备可见性` 与 `当前会话存活` 现在被更明确地拆成了两套状态

- 首页常驻连接提示继续减噪
  - 首页不再内联显示这类连接层提示：
    - `远端设备尚未选择目标端目录`
    - `远端设备已断开连接，请保持目标设备前台运行后重连`
  - 这类信息后续如需保留，应考虑放到更轻的反馈通道，而不是主流程区

- 步骤 3 重复说明文案清理
  - 删除了一批已无界面入口、且信息密度较低的说明键，包括：
    - 自动预览等待 / 运行 / 就绪说明
    - 目标端索引等待说明
    - `请先选择目录` / `请先连接远端设备` 这类重复流程提示
  - 保留真正仍在错误链路中使用的异常键

这一轮之后，首页步骤 1 与 discovery 的当前职责已经更明确：

- 步骤 1 负责“看到设备 / 进入会话”
- 二级面板承接“手动连接与辅助连接能力”
- discovery 负责“设备出现 / 下线感知”
- connection 负责“当前会话是否仍真实存在”

## 27. 自动监听默认化第一步

这一轮先不继续扩大步骤 1 的交互范围，而是把“监听”收回到更接近默认基础能力的位置。

已完成：

- 自动监听默认值改为开启
  - 当本地还没有保存过该设置时
  - `MusicSync` 会默认按“启动后自动进入监听”处理
- 应用启动自动监听链路继续沿用现有 `app.dart`
  - 设置加载完成后
  - 若当前开启了自动监听，且还未处于监听状态
  - 会自动触发一次 `startListening()`
- 二级面板中的“监听”入口改名为 `监听端口`
  - 这个入口当前只表达“配置监听端口”
  - 不再把它误读成步骤 1 里的另一个主流程动作
- 设置页文案同步收口
  - `自动监听` 改为 `启动时自动监听`
  - 描述也改成更直接的启动语义

这一轮的方向是：

- 继续淡化“先理解监听，再开始使用”这套心智负担
- 让监听更像应用的默认底层能力
- 把用户注意力继续留在“看到设备 / 建立会话 / 进入同步”上

## 28. 弹窗壳统一与监听面板收口

这一轮没有继续扩首页主流程，而是把一批已经开始产品化的弹窗结构收回到统一壳层，避免后续每个弹窗都各自维护尺寸、标题、内容区与底部按钮区。

已完成：

- 新增全局弹窗壳
  - `lib/app/widgets/app_dialog_shell.dart`
  - 当前提供：
    - `micro / compact / form / panel` 四档尺寸
    - 统一圆角、内边距、按钮尺寸
    - 标题栏可选关闭按钮
    - 无 actions 时自动收掉底部动作区留白

- 设置页原有弹窗壳已接到全局壳
  - `settings_dialog_shell.dart` 现在只保留设置页自己的轻量包装职责
  - 通用布局由 `AppDialogShell` 承担

- 首页监听面板已正式独立
  - 新增 `connection_status_dialog.dart`
  - 顶部监听胶囊现在打开的是统一风格对话框，而不是直接在原位切换复杂状态
  - 面板当前聚焦为 `端口配置`
    - 端口卡片可直接点击进入行内编辑
    - 确认按钮改为输入框右侧圆形确认按钮
    - `开始监听 / 停止监听` 使用同尺寸双状态按钮
  - 监听状态文案同步减噪
    - 顶部胶囊中的 `已连接并监听` 收短为 `已连接`

- 首页常用编辑弹窗已统一到同一套壳
  - 最近备注编辑
  - 最近地址编辑
  - 更多连接方式面板
  - 最近目录管理
  - 最近地址管理
  - 分享地址二维码弹窗

- 小尺寸特例能力已补齐
  - 二维码分享弹窗原本是一个特制窄卡片
  - 当前通过 `AppDialogSize.micro` 收进统一壳
  - 保留了原有窄宽度观感，不再需要单独手写 `Dialog + ConstrainedBox`

这一轮之后，首页与设置页的弹窗结构已经有了明确分层：

- 业务组件只负责内容与行为
- 统一弹窗壳负责尺寸、间距、动作区和关闭能力

这意味着后续如果继续做视觉微调，优先只需要改壳层，而不是回到各个弹窗里逐个追布局。

另外顺手补了一层确认类弹窗统一：

- 新增 `lib/app/widgets/app_confirm_dialog.dart`
- 当前先接入“同步前存在删除项”的确认场景
  - 首页本地预览执行确认
  - 远端同步执行确认

这样删除确认不再散落为重复的 `AlertDialog`，后续如果继续补危险操作确认，也可以直接沿用同一组件。

## 29. 音频标签读取扩展第一阶段

这一轮主要补的是“详情面板里的音频信息不要再只对 MP3 友好”，目标不是一次做成完整音乐资料库，而是先把常见格式与常见字段补到真正可用。

已完成：

- 音频容器识别从“默认走 ID3 兜底”调整为“先识别容器再解析”
  - 当前优先按文件头识别：
    - `FLAC`
    - `Ogg/Vorbis`
    - `Opus`
    - `MP4 / M4A`
  - 只有未命中这些容器时，才继续回退到原有 `ID3v2 / ID3v1`

- 已补支持的标签容器
  - `FLAC`：Vorbis Comment
  - `Ogg/Vorbis`
  - `OpusTags`
  - `MP4 / M4A`：`moov/udta/meta/ilst`
  - `APEv2 footer`

- 已补齐的音频字段
  - 原有：
    - 标题
    - 歌手
    - 专辑
    - 歌词
  - 新增：
    - 作者 / 作曲
    - 曲目号
    - 碟片号

- 文本编码也顺手收了一轮
  - Vorbis Comment 当前统一按 `UTF-8` 解码
  - 已确认修复一类 `FLAC / Ogg / Opus` 中日文文本乱码问题

- 展示链路已同步打通
  - 本地详情读取
  - 远端详情回传
  - 详情面板展示
  - 中英文字段标签

这一轮之后，详情面板中的音频信息不再明显偏向 `mp3`，而是已经能覆盖一批更接近真实音乐库使用场景的常见文件类型。

补一条本轮验证经验：

- 音频元数据相关改动在双端场景下验证时，需要两端都重新部署最新构建
- 否则很容易出现：
  - 本机详情已经正常
  - 远端详情仍返回旧逻辑结果
  - 最终看起来像是“同一个文件仍然乱码”

这类现象优先应先排除“远端仍在运行旧版本”，再判断是否还有真实解析问题。

## 30. 连接状态语义拆分与监听面板修正

这一轮主要处理的是顶部状态胶囊、监听面板以及连接控制器之间的语义打架问题。

已完成：

- 连接态与监听态正式拆分
  - `ConnectionState.status` 不再同时承担“连接是否存在”和“是否正在监听”两种语义
  - 新增独立字段 `isListening`
  - 顶部状态胶囊、监听面板按钮、启动自动监听逻辑都改为读取 `isListening`

- 修复监听面板按钮状态不稳定的问题
  - 之前若刷新最近记录、发现设备列表变化、收到目录状态等分支重建 `ConnectionState`
  - 某些分支会漏传 `isListening`
  - 导致重新打开监听面板时，`开始监听 / 停止监听` 文案会随机错乱
  - 当前所有主要状态更新分支都已补齐该字段透传

- 监听按钮当前语义已改为更直观的“下线”
  - 用户选择 `停止监听` 时
  - 现在会同时：
    - 广播 `goodbye`
    - 断开当前会话
    - 关闭 listener
    - 清空当前远端状态
    - 回到 `idle`
  - 也就是说，当前产品语义不再是“停止接受新连接但保留现有连接”
  - 而是更符合当前用户心智的“整体下线”

这一步的结果是：

- 顶部状态胶囊与监听面板终于说的是同一件事
- 用户点击“停止监听”后，不会再看到另一端还莫名保持连接的混乱状态

## 31. 步骤 2 目录变更链路补强

这一轮主要补的是“连接已建立后，某一端稍后再选择目录，另一端是否能真正拿到这次变化”。

已完成：

- 握手阶段继续保留目录就绪信息
  - 主动连接时会把本机当前 `directoryReady` 一并带到握手里
  - 因此“先选目录再连接”的场景已可直接同步目录就绪态

- 已连接后的目录变化继续通过会话消息主动同步
  - 本机目录选择 / 切换最近目录 / 清空目录
  - 最终都会走 `handleLocalDirectoryChanged(...)`
  - 并向当前对端发送 `remoteDirectoryChanged`

- 收到 `remoteDirectoryChanged(isReady: true)` 后
  - 不再只更新“远端目录已就绪”布尔状态
  - 现在会继续主动刷新一次远端快照
  - 这样不会再停留在“状态知道了，但目录内容仍然没拉下来”的半同步状态

- 被动接入一侧的刷新链路也已补齐
  - 之前被动端在尝试刷新远端快照时，会错误走主动连接 service session
  - 这会把“没有主动 session”误判成“对端断开”
  - 当前已改为：
    - 主动连接端：走 `_service.requestRemoteScan()`
    - 被动接入端：走 `_activeIncomingSession.sendRequest(scanRequest)`
  - 修复了“一端选目录后，另一端立即掉回监听中”的异常

这一轮之后，步骤 2 当前可视为具备了更完整的双阶段同步能力：

- 先选目录再连接
- 先连接再选目录
- 清空目录

这三类主路径都能把“目录是否就绪”以及“远端目录快照”更稳定地传递到另一端。

## 32. 自动远端预览触发移除

这一轮没有扩展预览能力，而是主动删掉了一层已经开始干扰主流程的自动行为。

已完成：

- 首页移除了 `_maybeScheduleAutoRemotePreview(...)` 整套自动触发链路
- 不再因为以下事件自动生成远端预览：
  - 连接状态变化
  - 目录状态变化
  - 远端目录刚就绪

当前收口后的原则是：

- 连接层负责建立会话与同步目录状态
- 预览层负责在用户明确操作后生成结果

这样做的原因很直接：

- 当前连接稳定性仍在继续验证阶段
- 自动生成预览会放大“刚连上 / 刚选目录 / 刚同步状态”时的抖动感
- 主流程上也更符合当前产品心智：
  - 先连上
  - 再选目录
  - 最后由用户明确触发预览

这一轮之后，远端预览当前只保留手动生成入口。

## 33. Android 保活第二阶段（常规锁增强）

这一轮没有进入更重的 Android 专项重构，而是先补上一组更常见、也更容易验证收益的保活增强。

已完成：

- 在原有前台服务基础上继续增加：
  - `WakeLock`
  - `WifiLock`

- `ConnectionKeepAliveService` 当前行为：
  - 启动时：
    - 拉起前台服务通知
    - 申请 `PARTIAL_WAKE_LOCK`
    - 申请 `WIFI_MODE_FULL_HIGH_PERF` 的 `WifiLock`
  - 销毁时：
    - 释放两种锁
    - 移除前台通知

- Manifest 已同步补齐权限：
  - `android.permission.WAKE_LOCK`
  - `android.permission.ACCESS_WIFI_STATE`

这一轮的目标不是承诺“后台连接绝对稳定”，而是先把常见系统层限制再向后推一步：

- CPU 不要太快休眠
- Wi‑Fi 不要太快降级
- 同步中的后台连接尽量延长存活概率

当前边界仍然明确：

- 这仍然不是“把整个同步逻辑搬到 Android Service 中托管”
- 只是把现有前台服务保活，从“只有通知”增强为“通知 + 常见系统锁”
- 最终稳定性仍建议以 release 实机验证为准，尤其是：
  - 已连接但未同步
  - 正在同步时切后台
  - 锁屏
  - Wi‑Fi 变化

## 34. 连接层转向 HTTP 控制面

这一轮先不再继续深补 raw socket 长连接，而是明确下一阶段的主方向：把连接层逐步迁到 `HTTP 控制面 + 独立传输面`。

这样决定的原因很明确：

- 当前连接会话同时承担了：
  - 握手
  - 目录状态同步
  - 远端扫描
  - 详情查询
  - 传输控制
- 一旦底层 socket 状态脏掉，就容易出现：
  - 表面已连接但实际已断
  - `Broken pipe`
  - 目录状态与真实会话不同步
- 后续产品方向又已经明确需要：
  - 更快的连接建立
  - HTTP 类接口
  - 并发请求

因此，继续在当前 raw socket 协议上堆越来越多补丁，性价比开始下降。

下一阶段的目标改为：

- `发现层`
  - 继续保留 UDP / 广播，用于发现设备
- `控制层`
  - 改为 HTTP 接口，承担：
    - `hello / register`
    - `directory status`
    - `scan`
    - `entry detail`
    - `sync prepare / cancel`
- `传输层`
  - 后续再决定继续走：
    - 分块 HTTP
    - 或独立传输通道

本轮开始的第一步不是立刻替换现有流程，而是先立 HTTP 控制面的骨架与 DTO，避免之后继续在旧协议上无序扩展。

随后继续推进到可运行阶段，当前主流程已经进一步收口为：

- `discovery`
  - 仍保留 UDP / 广播
- `control plane`
  - 已切到 HTTP：
    - `hello`
    - `session close`
    - `directory status`
    - `scan`
    - `entry detail`
    - `sync session state`
    - `begin / write / finish / abort copy`
    - `delete entry`
- `状态同步`
  - 不再依赖 socket 推送目录就绪状态
  - 当前改为连接期间轻量轮询 HTTP `directory status`
  - 若发现远端从“未准备好”变成“已准备好”，会继续自动刷新一次远端快照

同时也补了一处关键修正：

- 被动接入一侧在收到 HTTP `hello` 时
  - 不再错误使用请求体中的空地址
  - 而是直接记录 HTTP 请求来源地址
  - 这样后续目录状态查询与远端扫描才真正有目标可查

当前阶段的结果是：

- 设备连接与目录状态的主流程，不再依赖一条常驻 raw socket 会话
- 之前那类：
  - 表面已连接但一会失效
  - `Broken pipe`
  - 远端后来选目录但本地拿不到
  的问题，底层触发面已经被明显缩小

当前仍保留的一部分旧 socket 代码，主要只作为仓库过渡遗留与测试兼容，不再是运行时主流程的核心依赖。

## 35. HTTP 加密开关收口

这一轮把 HTTP/HTTPS 切换相关的几个关键收口点补齐了：

- 设置页新增了 `HTTP 加密` 开关，默认开启自签名 HTTPS，关闭后退回 HTTP
- 发现、握手、远端控制请求与同步执行，已经统一透传 `httpEncryptionEnabled`
- 首页顶部状态拆成两个独立胶囊：
  - `监听中 / 未监听`
  - `已连接 / 连接中 / 未连接`
  避免把监听状态和连接状态继续揉在同一个入口里

同时补了两处更关键的行为修正：

- 切换加密开关时，不再先持久化再切运行态
  - 现在改为：
    - 先临时切到目标设置值
    - 执行网络重置
    - 成功后再写入持久化
  - 若网络重置失败：
    - UI 状态会回滚
    - 运行态会尝试按旧值再恢复一次
    - 不会留下“设置已保存但网络栈还停在旧协议”的半状态

- `resetNetworkStateForProtocolChange()` 增加了过渡态保护
  - 连接建立中
  - 正在执行同步
  - 正在接收同步
  这几种状态下会直接拒绝切换协议，避免在半流程中强行断连/重启监听

测试也同步补上：

- `SettingsController`
  - 加密配置加载
  - 加密配置切换成功路径
  - 网络重置失败后的回滚路径
- `ConnectionController`
  - 连接中禁止协议切换
  - 远端同步执行中禁止协议切换

当前这部分已经达到“本轮可收口”的程度。
但和 LocalSend 对齐后的最终安全形态还差一块独立工作：

- 证书 fingerprint / pinning
- 发现与手动连接的设备身份闭环

这部分暂时已在相关代码位置补了 TODO，后续单独推进。

## 36. Home 滚动收口与预览弹窗化准备

本轮重点不是继续扩功能，而是先把 Home 的滚动冲突与认知复杂度收口。

已完成：

- `HomePage` 组装结构已下沉为明确 section builder，主 `build` 只保留状态聚合与布局拼装
- 新增 `home_workspace_layout.dart`，把 Home 页面滚动责任和工作台区域边界做了第一步分离
- 修复了 `home_workspace_layout.dart` 中 Stateful 改造后的常量作用域问题
- 针对“预览列表滚动导致主窗口跳动”做了止血：
  - 回退 Home 主层平滑滚动
  - 保留主滚动 `ScrollController + depth == 0` 通知过滤

当前结论：

- 主页面做全局平滑滚动时，与预览区局部滚动存在高冲突风险
- 把预览工作台提取为独立弹窗，是更符合当前复杂度预算的方向

下一步计划已固化为文档：

- `docs/home-preview-dialog-extraction-plan.md`

该方案坚持：

- 复用现有 `PreviewWorkbenchSection` 与 dialog 体系
- 对齐 MD3 交互语义
- 不改业务状态流，仅做承载边界调整

## 37. 传输链路重构收口

这一轮把远端文件内容传输正式收口到了新的 HTTP 流式协议，当前状态如下：

- 控制面继续保留在 HTTP
  - `hello`
  - `directory status`
  - `scan`
  - `entry detail`
  - `sync session state`
  - `delete entry`
- 文件内容传输主路径已切到：
  - `copyFileStream`
  - 单文件单请求
  - 二进制 body
  - 请求头携带：
    - `x-remote-root-id`
    - `x-relative-path`
    - `x-file-size`
- 服务端接收侧已收口为：
  - 临时文件写入
  - 接收字节数校验
  - 成功后 rename
  - 失败或中断即清理临时文件

这轮同时完成了几项关键修正：

- `hello` 请求与响应都会携带 `transferProtocols`
- 被动连接建立后，也会同步更新对端能力状态
- 因此不再需要“双方都主动点一次连接”才能让 `stream-v1` 生效
- 同步日志中已补充协议标记，能直接看到当前是 `stream-v1`

收口后的重要取舍也已明确：

- 旧的 `begin / write / finish / abort` chunk RPC 复制链路已从运行时主流程删除
- 当前不再兼容旧版 `chunk-rpc` 文件内容传输协议
- 如果后续还要继续提升吞吐，优先方向不再是重做协议，而是：
  - Android `MethodChannel` 写入去 base64
  - 取消时主动中断正在进行的 HTTP 上传
  - 小文件有限并发与吞吐埋点

## 37. 音乐领域参考项目清单落档

为避免“讨论结论散落在对话中难回看”，新增了文档：

- `docs/music-reference-projects.md`

本次把以下内容固化为可执行清单：

- 已有借鉴基线确认：
  - InstallerX-Revived（UI 设计）
  - LocalSend（网络/文件传输）
- 音乐语义方向新增参考：
  - Picard / beets / TagLib / Harmonoid / Strawberry / Navidrome
- 优先策略：
  - 先收口标签一致性（Picard + beets + TagLib 思路）
  - 再推进播放器交互能力（Harmonoid / Strawberry）
- 并补了一版可直接转代码规则的最小字段集与冲突优先级建议，作为后续任务起点。

## 38. 首页/传输页语义重排与预览工作台拆分

本轮先收口信息架构与步骤语义，再继续处理设备发现稳定性问题。

已完成：

- 默认入口 `/` 已改为轻量概览页
  - 首页不再直接承载传输流程
  - 只保留状态概览与进入传输页入口
- 原重型 `HomePage` 已迁为独立 `TransferPage`
  - 连接
  - 源目录选择
  - 步骤 3 传输工作台
  统一收敛到传输页
- 传输页布局已改为始终单列上下排序
  - 不再因宽屏切成左右双栏
- 步骤 1 与步骤 2 已进一步压平
  - 去掉内层包裹与额外边距
  - 步骤 2 已改为“显示 + 选择”一体式目录选择器
- 步骤 3 已拆成两层职责：
  - `PreviewWorkbenchSection` 只保留工作台样式与动作区
  - 新增独立结果组件承载同步列表与冲突列表
- 预览页继续承载完整结果列表
  - 传输页仅保留步骤 3 工作台样式
- 步骤 3 主动作已收口为单一按钮：
  - `查看预览列表`
  - 若当前预览不存在或过期，则先触发生成，再进入预览页

当前状态：

- 页面语义已从“首页即工作台”切换为“首页概览 + 传输页执行”
- 预览工作台与结果列表的边界已清晰
- 下一步进入连接发现稳定性重构，重点处理：
  - 设备卡片地址闪烁
  - 发现列表的稳定聚合与去重策略
## 2026-04-11 Device Discovery Identity Refactor

- 新增设备发现稳定身份决策文档：`docs/decisions/device-discovery-stable-identity.md`
- 开始把本机 `deviceId` 从临时的 `deviceName:port` 切换为持久化稳定身份
- 目标是修复“多个设备只显示一个”和“发现地址闪烁”问题
- `ConnectionController` 已改为基于持久身份构建本机 `DeviceInfo`
- discovery 列表聚合已改为稳定 identity key，并保留旧数据回退 key
- 补充连接控制器单测，覆盖“同名多设备并存”和“同设备地址稳定”场景
- `flutter analyze` 与相关单测已通过

## 2026-04-12 传输页、预览页与执行结果收口

这一轮主要不是继续扩底层协议，而是把“入口层 / 详情层 / 执行结果”这条体验链真正收口。

已完成：

- 传输页步骤 3 已收口为单入口摘要卡片
  - 传输页不再承载完整预览结果列表
  - 当前只保留预览前可确认的信息：
    - 传输方向
    - 本地 / 远端目录就绪状态
    - 源目录预检风险弱提示
    - 进入预览页的单一主按钮
  - 这一步的职责已经明确为：
    - 判断现在能不能进入检查
    - 进入预览详情页

- 预览页语义已从“另一个步骤 3”改为“详情层”
  - 页面标题改为 `预览`
  - 顶部改成真正的 `摘要` 区
  - 结果列表单独收为 `计划项`
  - 这样结构稳定为：
    - 摘要
    - 计划项
    - 执行区

- 独立结果页已移除
  - 结果不再跳转到单独 `ResultPage`
  - 执行链路现在在预览页内闭环
  - 进度卡只在运行态显示过程
  - 任务结束后，同位置切换为结束态结果卡

- 执行结束卡最近又做了一轮结果表达收口
  - 顶部保留 `已处理：x / y`
  - 下方使用四个紧凑指标徽标：
    - 复制
    - 删除
    - 失败
    - 已处理数据量
  - 成功 / 取消 / 失败都统一通过同一块状态提示条表达
  - 取消提示文案当前为：
    - `同步被提前结束，已尝试清理同步临时文件。`
  - 并且取消说明已从错误态降为 warning 语义

- 执行统计链路已补齐“运行中实时累计”
  - `TransferProgress` 新增：
    - `copiedCount`
    - `deletedCount`
    - `failedCount`
  - 本地 / 远端执行器会在进度回调中持续回传这三个计数
  - `ExecutionController` 会在运行中把这些值实时写入 `state.result`
  - 因此手动停止后，不再默认显示 `复制 / 删除 / 失败 = 0`

- 同时修正了两个被结果卡放大的状态问题
  - 执行初始态现在会直接写入计划总量
    - 避免“立刻取消时显示 `已处理：0 / 0`”
  - `completed` 但有 `failedCount > 0` 时
    - 结果卡不再错误显示纯成功提示
    - 改为按非成功结果处理

- MD3 配色继续收口
  - 本轮执行结果卡没有引入新的硬编码语义色
  - 传输页入站同步遮罩已从 `Colors.black54` 改为
    - `colorScheme.scrim.withValues(alpha: 0.54)`

验证：

- `flutter gen-l10n`
- `flutter analyze`
- 相关 widget / unit tests 已补齐并通过

当前可把主流程理解为：

- 首页：概览与入口
- 传输页：连接与准备
- 预览页：检查、执行、结果闭环

如果后续没有新的产品方向变化，这一层页面职责不应再回退到“首页直接承载全部工作台”。

## 2026-04-12 预览页文件类型临时排除收口

本轮把预览页“文件类型临时排除”能力收到了当前实现口径，并同步补齐了交互、摘要语义和测试。

- `PreviewState` 新增 `excludedExtensions`
  - 仅表示当前预览会话内的临时排除状态
  - 不写入设置页全局忽略配置

- `PreviewController` 已统一接入临时排除过滤链路
  - 排除后不会再进入当前 `plan`
  - 排除后不会参与后续执行
  - 新建预览默认重置排除状态
  - 执行后自动刷新会保留当前排除状态

- 预览页文件类型 chip 已形成两层语义
  - 长按：真实排除 / 恢复，影响 `plan` 与执行
  - 点击：仅切换当前页面查看筛选，不改写 `plan`

- 交互表达已收敛到“少文案、强状态”
  - 已排除类型使用独立排除态样式
  - 保留短反馈文案：
    - `已排除 {ext}`
    - `已恢复 {ext}`
  - 不再依赖“长按可恢复”这类教学型提示

- 同步收口了摘要语义
  - 顶部摘要不再固定显示全量 `plan.summary`
  - 当前会跟随页面实际筛选结果变化
  - 文件类型筛选与分组筛选都会影响摘要
  - 这样避免“列表看的是局部，摘要显示的是全量”的语义错位

- 补齐了一处真实交互缺口
  - 普通态扩展名 chip 原本没有真正接上长按入口
  - 现已补回普通态 chip 的 `onLongPress`

- 测试已补齐到最小闭环
  - controller 单测覆盖：
    - 排除 / 恢复
    - 与全局忽略叠加
    - 重建重置
    - 刷新保留
  - widget 测试覆盖：
    - 长按普通类型进入排除态
    - 长按已排除类型恢复
    - 点击已排除类型不恢复
    - 顶部摘要跟随文件类型筛选变化

验证：

- `flutter analyze`
- `flutter test test/unit/preview/preview_excluded_extensions_test.dart`
- `flutter test test/widget/preview_page_extension_exclusion_test.dart`

## 2026-04-12 home-preview-dialog-extraction-plan 归档

本轮对 `docs/home-preview-dialog-extraction-plan.md` 做了状态判断并归档，原因是该方案已被后续实现与进度记录覆盖，且其中“结果跳转独立 `ResultPage`”等描述已不再符合当前实际。

当前统一口径如下：

- 计划类文档若进入“已完成且后续语义变化”状态，优先归档，避免和现状并存造成误导
- 事实状态与演进历史统一以 `docs/progress-log.md` 为主线
- 涉及本主题的有效记录入口为：
  - 第 36 节（Home 滚动收口与预览拆分准备）
  - 第 38 节（首页/传输页语义重排与预览工作台拆分）
  - `2026-04-12 传输页、预览页与执行结果收口`

## 2026-04-13 冲突处理流 Phase 1–3 部分实现

本轮把 `docs/preview-conflict-flow-v1.md` 中定义的冲突处理流推进到了 Phase 2 基本可用 + Phase 3 部分闭环的状态。

已完成：

- Phase 1：入口打通（全部完成）
  - 桌面端右侧工具区 `onViewConflicts` 已接入导航
  - 手机端预览页底部新增独立冲突入口卡
  - 冲突页独立路由 `/preview/conflicts` 已注册
  - 桌面 / 手机端均可通过单主入口进入同一路由

- Phase 2：冲突页基本可用（大部分完成）
  - 冲突处理列表已按三分型（跳过项 / 冲突项 / 可自动合并项）分组展示
  - 当前冲突详情已实现桌面端左右并排对比（本地 / 远端）和手机端 sheet + 本地/远端切换
  - 返回预览页已接入
  - 桌面左侧主列表已移除 `conflict` 分组（筛选 chip + 列表项均已移除）
  - 手机端旧冲突路径已收口（`PreviewResultListSection` 和 `PreviewPlanSection` 不再传递冲突项）
  - 快照首轮分类链路尚未接入（当前 `FileEntry.fingerprint` 未被扫描器赋值，`autoMerge` 分支为预留死代码）

- Phase 3：处理动作部分闭环
  - 基础处理动作入口已添加（稍后处理 / 保留源端 / 保留目标端 / 申请自动合并）
  - 处理动作已按"先记录草稿"落地，不立即改写 `plan`
  - 草稿绑定当前预览会话，重建预览后清空
  - 自动合并同意步骤、远端写入、二次复核尚未实现

- 执行反馈闭环已补齐
  - `TransferProgress` 和 `ExecutionResult` 新增 `skippedConflictCount`
  - 本地 / 远端执行器在执行开始时统计冲突项数量并透传
  - 执行结果卡新增"跳过冲突"指标徽标

- 冲突分类逻辑已实现 V1 版本
  - `ConflictController._classify()` 基于扩展名白名单 + size/modifiedTime + fingerprint 三级分类
  - 音乐文件扩展名白名单已提取到 `AppConstants.musicExtensions`
  - fingerprint 比较已提取到 `core/utils/fingerprint_utils.dart`（含 `normalizeFingerprint` + `fingerprintsMatch`）
  - 当前 fingerprint 字段未被扫描器赋值，`autoMerge` 分类暂不可达

- Discovery bind 失败优雅降级
  - `ConnectionController._startDiscoveryReceiving()` 包裹了 `discovery.startReceiving`
  - catch 后设置 `errorMessage` 而不是让异常 uncaught 导致 controller 崩溃
  - 新增 `AppErrorCode.discoveryUnavailable` 错误键与 l10n 文案
  - 补充了 `_ThrowingDiscoveryService` 单测

- 移动端布局修复
  - `PreviewConflictListPane` 和 `PreviewConflictDetailPane` 新增 `shrinkWrap` 参数
  - 移动端冲突页使用 `shrinkWrap: true` + `showModalBottomSheet` 替代内嵌详情

- 测试收口
  - `preview_page_lifecycle_test.dart` 已更新，移除过时断言
  - `execution_controller_test.dart` 新增 `skippedConflictCount` 透传测试
  - `connection_controller_test.dart` 新增 discovery 降级测试

- Fingerprint 方案文案已收口到文档 §6.5
  - partial hash 定位为"显著降低误判"，不承诺强一致
  - autoMerge 语义限定为"可自动归并为同内容候选、默认不打扰用户"，不跳过最终确认
  - 兼容性规则已补清：protocolVersion 守卫 + 字段有效性校验 + 统一回退到 size/mTime
  - 建议实现时把协议支持判断、字段有效性、autoMerge 许可收敛到单一 helper

- Fingerprint 链路已实现
  - 新增 `xxh3` 依赖，用于 XXH3-64 快速哈希
  - 新增 `fingerprint_compute.dart`：`computePartialFingerprint()` 从文件流读取首部 64KB 计算 XXH3 哈希，2 秒超时兜底
  - `DirectoryScanner._walk()` 对非目录项调用 `computePartialFingerprint` 写入 `FileEntry.fingerprint`
  - `AppConstants.protocolVersion` 从 1 升至 2，新增 `fingerprintSupportedVersion = 2` 和 `fingerprintSampleSize = 64KB`
  - `DeviceInfo` 新增 `protocolVersion` 字段，默认 1（兼容旧版）
  - `HelloRequestDto` / `HelloResponseDto` 透传 `DeviceInfo.protocolVersion`
  - `DiscoveryService` 广播和接收均携带 `protocolVersion`
  - `DiscoveredDeviceEntry` 新增 `protocolVersion`，透传到 `toDeviceInfo()`
  - `ConnectionController` 所有 `DeviceInfo` 构造点均已补 `protocolVersion`
  - 新增 `canUseFingerprintClassification()` 守卫 helper（收敛协议版本 + 字段有效性 + 匹配判定）
  - `ConflictController._classify()` 使用 `canUseFingerprintClassification` 替代原来的 `fingerprintsMatch`，旧版对端自动回退到 `conflict` 分类
  - 测试：`fingerprint_compute_test.dart`（5 例）、`fingerprint_utils_test.dart`（10 例含守卫）、`conflict_controller_test.dart`（4 例含旧版回退 + 缺失回退）

- 冲突详情字段级对比 + autoMerge 依据展示
  - `AudioMetadataViewData.diffFields()` — 标签字段差异集合计算（7 字段逐一归一化比较）
  - `_PanelValueRow` / `_PanelLyricsValueRow` 新增 `highlight` 参数，差异字段使用 `errorContainer` 底色 + `swap_horiz` 图标
  - `_PanelEntryCard` 新增 `diffFields` 参数，从 `DiffItemDetailPanelContent` 自动计算并透传
  - `DiffItemDetailPanelContent` 新增 `overrideDiffFields` 参数，供并排布局外部传入
  - `_SidePanel` 透传 `diffFields` 到 `DiffItemDetailPanelContent`
  - `_FingerprintHitBadge` — autoMerge 项展示指纹匹配说明（tertiaryContainer 配色 + fingerprint 图标）
  - `PreviewConflictDetailPane` 新增 `category` 参数，当 `autoMerge` 时显示 badge
  - `preview_conflict_page.dart` 桌面/移动端均传入 `category`
  - l10n：`conflictFingerprintHit`（EN: "Fingerprint match — content likely identical", ZH: "指纹匹配 — 内容可能相同"）
  - 测试：`diff_item_detail_view_data_test.dart`（7 例）
- 差异高亮语义修正
  - `_effectiveDiffFields()` — 刷新后两侧 metadata 齐全时优先内部重算 diff，不再卡在初始快照；刷新前回退 `overrideDiffFields`
  - `_PanelLyricsValueRow` 补齐高亮渲染（底色 + 前景色 + swap_horiz 图标），与 `_PanelValueRow` 视觉语言统一

当前仍待完成：

- 自动合并同意步骤
- 默认只写远端 + 可选双向写入
- 不确定项二次详情复核

## 2026-04-13 冲突处理流文档最终收口

本轮把 `docs/preview-conflict-flow-v1.md` 从“方向草案”收口到“可直接进入实现”的口径。

已固定的关键决策：

- 预览执行维持：`copy/delete` 可执行，冲突处理项先跳过
- 冲突处理页使用独立路由（建议：`/preview/conflicts`）
- 桌面端主入口收口到右侧工具与操作区；手机端保持单主入口紧凑表达
- 冲突处理状态绑定当前预览会话，重建预览后清空
- V1 动作统一为“先记录草稿”，不立即改写当前 `plan`
- 自动合并默认只写远端，且必须经过用户同意
- 元数据链路采用“扩展快照后批量比对 + 按需详情补充”
- 详情中的 Tag 字段改为常驻显示，缺失值统一“未知”
- 非音乐文件与 Tag 读取失败不再视为流程错误

同时明确了过渡退出条件：

- 冲突处理页达到 Phase 2（列表 + 详情 + 返回）后，桌面左侧主列表移除 `conflict` 分组

验证：

- `flutter analyze`
- `flutter test test/unit/preview/local_audio_metadata_reader_test.dart`
